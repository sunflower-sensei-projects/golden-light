/// Add a User Event 1 (or whatever event_user(1) maps to in your
/// project's convention) with whatever the quake reaction should be
/// (topple, destroy, open a path, etc.) — this is where
/// fs_apply_quake's event_user(1) call lands.